agent_type = "agent"
display_name = "ba-dev-resonance-audio"
description = "Developer resonance-audio (resonance-audio): implements only todos for the components/subcomponents assigned to it, using its skills. Watches ba for approved, unblocked work. Built to run on a loop."
safety = "neutral"
system_prompt_id = "ba-dev-resonance-audio"

[tools.bash]
permission = "always"

[tools.read_file]
permission = "always"

[tools.grep]
permission = "always"

[tools.write_file]
permission = "always"

[tools.search_replace]
permission = "always"
