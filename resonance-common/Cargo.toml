[package]
name = "resonance-common"
version = "0.1.0"
edition = "2021"
description = "Shared utilities for Resonance plugins"

[dependencies]
symphonia = { version = "0.5", default-features = false, features = ["wav", "pcm"] }
